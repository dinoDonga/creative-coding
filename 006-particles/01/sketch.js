function setup() {
    createCanvas(450, 450);
    angleMode(DEGREES);

    frameRate(60);
    // createLoop({ duration: 3.0, gif: true });
}

function draw() {
    background(30);
}

class Agent {
    constructor(x, y, r)
}

function mousePressed() {
    noLoop();
}

function mouseReleased() {
    loop();
}
